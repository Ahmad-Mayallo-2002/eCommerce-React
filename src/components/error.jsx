import { Container } from "react-bootstrap"
import { useParams } from "react-router-dom";

export const Error = () => {
    return (
        <section className="error my-5">
            <Container>
                <h1>Error</h1>
            </Container>
        </section>
    )
};